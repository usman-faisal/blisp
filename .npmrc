# This hoists key React Native dependencies so Metro can find them
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*expo*
public-hoist-pattern[]=*react-native*
public-hoist-pattern[]=*metro*
# Optional: Use 'hoisted' if you experience persistent native build issues
# node-linker=hoisted
